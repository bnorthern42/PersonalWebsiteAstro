---
const { title, description } = Astro.props;
---
<div class="card hover:scale-105 transition-transform duration-300">
  <h3 class="text-xl mb-2">{title}</h3>
  <p class="text-secondary mb-4">{description}</p>
  {/* Add tech badges if wanted: <span class="bg-gray-200 px-2 py-1 rounded text-sm">React</span> */}
</div>